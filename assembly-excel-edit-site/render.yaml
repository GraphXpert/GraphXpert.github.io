services:
  - type: web
    name: assembly-excel-edit-web
    env: node
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: MAX_FILE_SIZE
        value: 52428800
    healthCheckPath: /health
